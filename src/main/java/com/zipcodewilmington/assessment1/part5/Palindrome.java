package com.zipcodewilmington.assessment1.part5;

import com.zipcodewilmington.assessment1.part1.BasicStringUtils;
import com.zipcodewilmington.assessment1.part2.StringUtils;

import java.util.ArrayList;
import java.util.Arrays;

public class Palindrome {

    public Integer countPalindromes(String input) {
       /* String[] inputArray = input.split("");
        ArrayList<String> inputList = new ArrayList<>(Arrays.asList(inputArray));
        for (int i = 0; i < input.length(); i++) {
            for (int j = i + 1; j <= input.length(); j++) {
                inputList.add(input.substring(i, j));
            }
        }
        for (int k = 0; k < inputArray.length; k++) {
            if (inputList.get(k).length() == 1)
                inputList.remove(k);
        }
        System.out.println(Arrays.toString(inputList.toArray()));
        int count = 0;
        for (int l = 0; l < inputList.size(); l++) {
            if (isPalindromic(inputList.get(l)))
                count = count + 1;
        }
        return count -1;
    }

    public static boolean isPalindromic(String str) {
        BasicStringUtils stringUtils = new BasicStringUtils();
        String reversedStr = stringUtils.reverse(str);
        return str.equals(reversedStr); // cleaner code
    }*/
        return null;
    }
}

